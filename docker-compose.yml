# Run from the repo root:
#   docker compose up -d --build
# Then point nginx `location /mcp` at 127.0.0.1:8791 (see deploy notes / nginx-mcp.conf).
services:
  orion-mcp:
    build:
      context: .
    container_name: orion-mcp
    restart: unless-stopped
    ports:
      - "127.0.0.1:8791:8791"
